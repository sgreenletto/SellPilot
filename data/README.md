# Data

本目录用于后续阶段的数据资产管理，当前不包含业务数据。

- `raw/`：原始输入数据；实际数据默认不纳入 Git。
- `processed/`：清洗或转换后的数据；实际数据默认不纳入 Git。
- `demo/`：可公开、脱敏且获准提交的演示数据。
- `templates/`：数据导入、导出或生成任务所需的模板。

禁止提交真实用户数据、店铺数据、个人信息、密钥或其他敏感内容。演示数据必须为合成数据或完成不可逆脱敏并经人工确认的数据。
