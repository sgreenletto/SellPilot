# Deployment

本目录维护环境、配置和 Demo 部署说明：

- `local-setup.md`：Windows + PostgreSQL 本地初始化与门禁。
- `knowledge-base-initialization.md`：知识数据幂等导入、状态检查和真实命中验收。
- `docker-compose.md`：PostgreSQL、backend、frontend 容器编排。

当前配置面向 Mock Shopee Demo，不是生产部署方案。备份、扩缩容、监控和真实平台配置
仍属于后续范围。
