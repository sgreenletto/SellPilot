# SellPilot Agent Guidelines

本文件约束后续在本仓库中执行任务的 Codex 或其他自动化代理。

## 开始任务前

1. 修改代码前先阅读根目录 `README.md`、相关需求文档和架构文档。
2. 明确任务所属阶段，不得把规划中的能力描述为已实现或可运行。
3. 保持首版单用户、单模拟店铺、Mock Shopee 和平台适配器边界。

## 设计与实现约束

1. Shopee 相关能力必须通过平台适配器抽象访问；业务代码不得直接依赖 `MockShopeeAdapter` 的具体实现。
2. `RealShopeeAdapterStub` 只能表达预留接口，不得伪装或暗示真实 Shopee 已接入。
3. 后端业务逻辑不得直接堆在 API 路由中；路由只负责协议转换、校验和调用应用层。
4. AI 输出必须使用明确的结构化 Schema，并在进入业务流程前完成校验与错误处理。
5. 所有业务写操作必须先进入待确认任务，只有获得明确确认后才能执行。
6. 新功能必须包含与风险相匹配的必要测试，并同步更新相关需求、架构、API 或运维文档。
7. 正式数据库结构只能通过 Alembic 迁移维护；不得在应用启动时调用 `create_all`。
8. AgentTask 和 ConfirmationTask 只能由内部 Service 创建，不得绕过服务层开放公共创建 API。

## 安全约束

1. 不得提交密钥、API Key、Token、密码、`.env`、真实个人数据或真实店铺数据。
2. 示例、日志、截图和测试数据必须使用合成内容或经过不可逆脱敏的内容。
3. 不得绕过适配器、确认流程、Schema 校验或其他安全边界以追求临时可用。

## Git 操作约束

1. Codex 不得自行执行 `git commit`、`git push`、创建 Tag 或修改远程仓库。
2. 遵循 `docs/git-workflow.md` 中的分支、Pull Request 和提交信息约定。
3. 保留用户已有变更，不覆盖或删除与当前任务无关的内容。

## 任务结束报告

每次任务结束必须如实报告：

- 创建或修改的文件；
- 执行过的命令；
- 检查或测试结果；
- 未解决问题、风险和需要人工确认的事项。
